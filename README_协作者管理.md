# 📚 多宝宝协作者管理 - 核心交付文件索引

## 🎯 为什么需要这套系统？

在多宝宝管理场景中，面临的核心问题：
- ❓ 多个协作者如何安全访问同一个宝宝？
- ❓ 不同协作者的权限如何控制？
- ❓ 如何在保护隐私的同时提高协作效率？
- ❓ 临时协作者（如月嫂）的权限何时自动收回？

**本套系统完全解决这些问题！**

---

## 📦 完整交付物清单

### 📄 设计文档（3份）

| 文件名 | 用途 | 重点 |
|--------|------|------|
| **协作者管理设计方案.md** | 完整的系统设计文档 | 架构、流程、API设计 |
| **多宝宝协作者管理实现清单.md** | 实现步骤和集成指南 | 代码集成、测试场景 |
| **协作者管理系统架构图.md** | 可视化架构说明 | ER图、时序图、决策树 |

### 💻 核心代码文件（6份）

#### 类型定义（1份）
```
src/types/collaborator.ts
├─ BabyCollaborator (协作者信息)
├─ MyPermission (用户权限)
├─ RolePermissions (角色权限)
├─ InviteCollaboratorRequest (邀请请求)
├─ InviteCollaboratorResponse (邀请响应)
└─ 工具函数 (类型转换、校验)
```

#### 工具函数（1份）
```
src/utils/permission.ts
├─ canViewBaby() ..................... 能否查看
├─ canEditRecords() .................. 能否编辑记录
├─ canManageBaby() ................... 能否管理宝宝
├─ canManageCollaborators() .......... 能否管理协作者
├─ canInviteCollaborators() .......... 能否邀请
├─ hasPermission() ................... 检查特定权限
├─ isPermissionExpired() ............. 是否过期
├─ getExpirationWarning() ............ 过期警告
├─ getRoleLabel() .................... 获取角色标签
├─ getRoleDescription() .............. 获取角色描述
├─ getRolePermissionsList() .......... 获取权限列表
├─ getPermissionDescription() ........ 获取权限描述
└─ formatPermissionText() ............ 格式化权限文本
```

#### API 接口（1份）
```
src/api/collaborator.ts
├─ apiFetchCollaborators() ........... 获取协作者列表
├─ apiInviteCollaborator() ........... 邀请协作者
├─ apiRemoveCollaborator() ........... 移除协作者
├─ apiUpdateCollaboratorRole() ....... 更新权限
├─ apiGetInvitationByCode() .......... 获取邀请详情
├─ apiJoinBaby() ..................... 确认加入
└─ apiBatchInviteCollaborators() ..... 批量邀请(可选)
```

#### UI 组件（1份）
```
src/components/BabyCollaboratorsPreview.vue
├─ 显示协作者总数
├─ 列表显示前3个协作者
├─ 超过3个显示"+N"标签
└─ 支持点击进入管理页面
```

#### 页面实现（1份）
```
src/pages/baby/collaborators/collaborators.vue
├─ 完整协作者列表
├─ 搜索/筛选功能
├─ 权限变更对话框
├─ 移除确认对话框
├─ 邀请新协作者入口
└─ 权限信息展示
```

---

## 🔑 核心概念速查

### 三大数据结构

#### 1. BabyCollaborator (协作者)
```typescript
{
  openid: string;              // 用户唯一ID
  nickName: string;            // 用户昵称
  avatarUrl?: string;          // 用户头像
  role: 'admin'|'editor'|'viewer'; // 权限角色
  accessType: 'permanent'|'temporary'; // 权限类型
  expiresAt?: number;          // 过期时间戳(秒)
  joinedAt: number;            // 加入时间戳(秒)
  isCreator: boolean;          // 是否为宝宝创建者
}
```

#### 2. MyPermission (用户权限)
```typescript
{
  babyId: string;              // 宝宝ID
  role: 'admin'|'editor'|'viewer'; // 权限角色
  accessType: 'permanent'|'temporary'; // 权限类型
  expiresAt?: number;          // 过期时间戳(秒)
  joinedAt: number;            // 加入时间戳(秒)
}
```

#### 3. RolePermissions (角色权限)
```typescript
{
  role: string;                // 'admin'|'editor'|'viewer'
  label: string;               // 角色显示名
  description: string;         // 角色描述
  permissions: string[];       // 具体权限列表
}
```

### 三个权限等级

| 等级 | 权限标签 | 能做什么 | 代表人物 |
|------|----------|----------|----------|
| **admin** | 管理员 | 完全控制 | 宝宝创建者 |
| **editor** | 编辑者 | 查看+编辑+邀请 | 另一位父母 |
| **viewer** | 查看者 | 仅查看 | 爷爷奶奶 |

### 权限有效期

| 类型 | 含义 | 场景 | 自动过期 |
|------|------|------|----------|
| **permanent** | 永久有效 | 家庭成员 | ❌ 否 |
| **temporary** | 有时限 | 月嫂、保姆 | ✅ 是 |

---

## 🚀 快速开始（5分钟集成）

### 1️⃣ 复制文件（30秒）
```bash
# 已生成在以下位置：
✅ src/types/collaborator.ts
✅ src/utils/permission.ts
✅ src/api/collaborator.ts
✅ src/components/BabyCollaboratorsPreview.vue
✅ src/pages/baby/collaborators/collaborators.vue
```

### 2️⃣ 注册页面（30秒）
在 `src/pages.json` 中添加：
```json
{
  "path": "pages/baby/collaborators/collaborators",
  "style": { "navigationBarTitleText": "管理协作者" }
}
```

### 3️⃣ 更新宝宝列表（2分钟）
在 `src/pages/baby/list/list.vue` 中：
```vue
<!-- 导入组件 -->
<template>
  <BabyCollaboratorsPreview
    :baby-id="baby.babyId"
    :collaborators="collaborators[baby.babyId]"
    @go-to-collaborators="goToCollaborators"
  />
</template>

<script setup>
// 加载协作者数据
const loadCollaborators = async () => {
  for (const baby of babyList.value) {
    const data = await apiFetchCollaborators(baby.babyId);
    collaborators[baby.babyId] = data;
  }
};
</script>
```

### 4️⃣ 添加权限检查（1分钟）
在需要权限的页面添加：
```typescript
import { canEditRecords } from '@/utils/permission';

onMounted(() => {
  const permission = getMyPermission(currentBabyId.value);

  if (!canEditRecords(permission)) {
    uni.showToast({ title: '您无权编辑' });
    uni.navigateBack();
  }
});
```

### 5️⃣ 测试（1分钟）
```bash
# 打开宝宝列表
# 点击宝宝的协作者预览
# 验证显示所有协作者
# ✅ 完成！
```

---

## 📖 学习路径

### 第一阶段：理解数据结构（15分钟）
1. 打开 `src/types/collaborator.ts`
2. 理解 `BabyCollaborator` 和 `MyPermission` 的结构
3. 查看 `ROLE_PERMISSIONS` 常量

### 第二阶段：掌握权限检查（20分钟）
1. 打开 `src/utils/permission.ts`
2. 逐个理解权限检查函数
3. 运行权限检查代码示例

### 第三阶段：学习 API 调用（15分钟）
1. 打开 `src/api/collaborator.ts`
2. 理解各个 API 的请求和响应格式
3. 查看错误处理方式

### 第四阶段：实现页面（30分钟）
1. 查看 `BabyCollaboratorsPreview.vue` 的实现
2. 查看 `collaborators.vue` 的完整实现
3. 根据需要进行定制修改

### 第五阶段：集成到项目（30分钟）
1. 按照快速开始的步骤进行集成
2. 运行测试验证功能
3. 调整样式和交互

---

## 🎓 代码示例速查

### 检查用户是否能编辑记录
```typescript
import { canEditRecords } from '@/utils/permission';
import { getMyPermission } from '@/store/baby';

const permission = getMyPermission(currentBabyId.value);

if (canEditRecords(permission)) {
  // 用户有权限，显示编辑按钮
} else {
  // 用户无权限，隐藏编辑功能
}
```

### 检查权限是否过期
```typescript
import { isPermissionExpired, getExpirationWarning } from '@/utils/permission';

if (isPermissionExpired(permission)) {
  // 权限已过期，隐藏宝宝
} else {
  const warning = getExpirationWarning(permission);
  if (warning) {
    // 权限即将过期，显示警告
  }
}
```

### 获取协作者列表
```typescript
import { apiFetchCollaborators } from '@/api/collaborator';

const collaborators = await apiFetchCollaborators(babyId);

// 按角色分组显示
const adminList = collaborators.filter(c => c.role === 'admin');
const editorList = collaborators.filter(c => c.role === 'editor');
const viewerList = collaborators.filter(c => c.role === 'viewer');
```

### 邀请协作者
```typescript
import { apiInviteCollaborator } from '@/api/collaborator';

const response = await apiInviteCollaborator(babyId, {
  targetOpenid: 'user_openid',
  role: 'editor',
  accessType: 'temporary',
  expiresAt: Math.floor(Date.now() / 1000) + 30 * 24 * 60 * 60, // 30天后
});

// response.qrCodeUrl - 二维码图片地址
// response.shortCode - 邀请短码
// response.token - 邀请令牌
```

### 变更权限
```typescript
import { apiUpdateCollaboratorRole } from '@/api/collaborator';

await apiUpdateCollaboratorRole(
  babyId,
  collaborator.openid,
  'viewer',  // 新角色
  undefined  // 改为永久权限
);
```

---

## 📊 实施时间表

| 阶段 | 任务 | 时间 | 负责人 |
|------|------|------|--------|
| 第1周 | 代码集成和页面注册 | 2-3天 | 前端开发 |
| 第2周 | 权限检查整合 | 2-3天 | 前端开发 |
| 第3周 | 测试和优化 | 3-4天 | QA + 前端 |
| 第4周 | 灰度发布 | 1-2天 | 产品 + 技术 |
| 第5周 | 监测反馈 | 持续 | 全体 |

---

## ✅ 上线前检查清单

### 功能测试
- [ ] 宝宝列表正确显示协作者预览
- [ ] 能进入协作者管理详情页
- [ ] 能正确变更协作者权限
- [ ] 能成功移除协作者
- [ ] 权限检查生效

### 权限测试
- [ ] Admin 能执行所有操作 ✓
- [ ] Editor 能添加记录但不能删除宝宝 ✓
- [ ] Viewer 只能查看不能编辑 ✓
- [ ] 过期权限被正确拒绝 ✓

### 性能测试
- [ ] 列表加载时间 < 1s
- [ ] 权限检查 < 10ms
- [ ] 页面切换流畅无卡顿

### 兼容性测试
- [ ] 各种屏幕尺寸
- [ ] iOS 微信小程序
- [ ] Android 微信小程序
- [ ] H5 浏览器

---

## 🔗 文件关联图

```
┌─────────────────────────────┐
│   设计文档                   │
│ • 设计方案.md               │
│ • 实现清单.md               │
│ • 架构图.md                 │
└────────────┬────────────────┘
             │
             ↓
┌─────────────────────────────┐
│   类型和工具                │
│ • collaborator.ts (类型)    │
│ • permission.ts (工具)      │
└────────┬─────────┬──────────┘
         │         │
         ↓         ↓
┌─────────────────┐  ┌──────────────┐
│  API 层         │  │  组件/页面   │
│ • collaborator  │  │ • Preview    │
│   .ts (接口)    │  │ • Detail     │
└────────┬────────┘  └──────┬───────┘
         │                  │
         └──────────┬───────┘
                    ↓
            ┌──────────────────┐
            │   使用示例       │
            │  各页面集成      │
            └──────────────────┘
```

---

## 🎁 额外的好处

### 为产品带来的价值
✅ 提高家庭成员的协作效率
✅ 保护宝宝隐私和数据安全
✅ 支持灵活的权限管理
✅ 适应多场景协作需求

### 为开发带来的优势
✅ 完整的代码框架，开箱即用
✅ 详细的文档说明，快速理解
✅ 权限检查模块化，易于维护
✅ 支持未来功能扩展

---

## 📞 常见问题

### Q: 权限信息存储在哪里？
**A:** 服务器存储在 `baby_collaborators` 表中，客户端存储在 store 的 `myPermissionsMap` 中。

### Q: 权限过期后会怎样？
**A:** 自动禁用权限，宝宝从列表隐藏，用户无法访问。可申请延期。

### Q: 能否同时是多个宝宝的协作者？
**A:** 可以，每个宝宝的权限是独立的。用户可以是 Baby1 的 admin，同时是 Baby2 的 viewer。

### Q: 如何处理协作者之间的权限冲突？
**A:** 不会有冲突，因为每个用户对每个宝宝的权限是独立维护的。

### Q: 临时权限到期后用户会知道吗？
**A:** 会的，系统会在过期前 7 天、1 天和过期时显示通知。

---

## 🚀 下一步行动

### 立即开始
1. 阅读 `多宝宝协作者管理完整解决方案.md`
2. 查看 `协作者管理系统架构图.md`
3. 按照 `多宝宝协作者管理实现清单.md` 进行集成

### 需要帮助
- 查看代码文件中的详细注释
- 参考 API 文档中的示例代码
- 查阅架构图理解数据流向

---

**📌 最后，祝集成顺利！** 🎉

有任何问题，请参考完整的设计文档或代码注释。
系统已经为您准备好了全部所需的功能，只需按照步骤集成即可！

